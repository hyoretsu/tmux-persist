#!/usr/bin/env expect

source "./tests/helpers/expect_helpers.exp"

expect_setup

spawn tmux
# delay with sleep to compensate for tmux starting time
sleep 1

start_persist_restore

# delete all existing persist save files
run_shell_command "rm ~/.tmux/persist/*"

start_persist_save

run_shell_command "tmux kill-server"
